apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: todo-app-route
spec:
  parentRefs:
  - name: $(TODO_APP_GATEWAY_NAME)
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: $(TODO_APP_SERVICE_NAME)
      port: 80
