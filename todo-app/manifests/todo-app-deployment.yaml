apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-app-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-app
  template:
    metadata:
      labels:
        app: todo-app
    spec:
      volumes:
      - name: todo-app-volume
        persistentVolumeClaim:
          claimName: todo-app-claim
      containers:
        - name: todo-app-server
          image: adamfarhadi/dwk-todo-app:2.2
          imagePullPolicy: Always
          env:
          - name: PORT
            value: "3001"
          volumeMounts:
          - name: todo-app-volume
            mountPath: /usr/src/app/files